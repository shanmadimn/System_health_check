global:
  scrape_interval: 5s # How often to check your Python script

scrape_configs:
  - job_name: 'python_health_agent'
    static_configs:
      - targets: ['localhost:8080'] # Points to your exporter.py
